# Progress

Phases from `MASTER_PLAN.md` §28. Status: `done` · `partial` · `not started`.

| Phase | Status | Notes |
| --- | --- | --- |
| 0 — Foundation | **done** | Monorepo, git hygiene, docs, docker-compose, Django, React, CI, env config, `/health/`, OpenAPI, tests. Dockerfile not yet built locally (no Docker on the dev machine). |
| 1 — Accounts & Authentication | **done** | Custom `Account`, email login, register/login/refresh/logout, `/me` (GET/PATCH), roles + capability registry, permission classes, password reset, email verification (timestamp state, no gating), Firebase exchange behind an adapter (disabled until the owner provides credentials — activation is configuration only), web routing + session layer + login/register/profile/forgot/reset/verify pages, full test coverage, docs. Deferred to later phases by design: admin account-management endpoints (Phase 10 admin dashboard), phone-only Firebase sign-in (ADR-018), password change for logged-in users. |
| 2 — Providers & Medical Services | **done** | Geography (Iraq seeded) and specialties reference data, ProviderProfile with controlled types and admin-only verification, memberships workflow, service offerings, public discovery with filters/pagination, owner self-management API, web search/detail/self-management pages, permissions and query-count tests. Deferred by design: distance search (needs PostGIS decision), media uploads for logos (media module), notifications on membership events (Phase 9), invitation UX by name search, admin dashboard UI (Phase 10). |
| 2.5 — Web Design System & Premium UI | **done** | Owned design system (tokens, CSS modules, icons, IBM Plex Sans Arabic), premium shell (header, mobile nav, footer), redesigned home, auth pages, profile, provider discovery/detail/workspace, 404. Loading rules preserved through `ApiActionButton`/`AsyncPage`. Deferred: Modal/Drawer/Dropdown/Tooltip/Toast/Breadcrumb (no page needs them yet), self-hosted font, dark theme. |
| 3 — Billing & Entitlements + Medical Jobs & Talent Marketplace | **done** | Reusable billing foundation (plans, entitlements, manual subscription activation, credits, atomic usage), employer organisations with admin verification and seat-limited memberships, structured job-seeker résumé (no uploads), job state machine with admin review, public job search, applications with snapshots, entitlement-gated talent search (one billable search per filter set per day), saved candidates, invitations, interviews, text-only messaging, contact-leak detection, admin console, audit log. Web: jobs search/detail/apply, my applications, seeker profile, employer workspace/job editor/applicants/talent, admin console, home Jobs section. Deferred: recruitment notifications (Phase 9), analytics, saved searches, seeker pricing, scheduled expiry job. |
| 4 — Reservations (free) | not started | Next. Owner decision: stays free. |
| 5 — Reviews & Offers | not started | |
| 6 — Medical Marketplace | not started | |
| 7 — Medical Real Estate | not started | |
| 8 — Advertising & Payments | not started | Gateway choice is the owner's decision. |
| 9 — Chat & Notifications | not started | |
| 10 — Dashboards & Analytics | not started | Includes admin account management. |
| 11 — Mobile (Flutter) | not started | `mobile/README.md` records the constraints. |
| 12 — Production Hardening | not started | Includes CSP, httpOnly refresh cookie evaluation, email provider, background email sending, blacklist pruning job. |

## Milestone log

- **2026-09-23** — Phase 0 complete; Phase 1 foundation (Account, JWT auth, `/me`) complete. 57 backend tests, 12 web tests. Pushed to https://github.com/moahaimen/enhanced_racheeta; CI green on `30f23ab`.
- **2026-09-23** — Phase 1 complete on branch `feat/phase1-auth-completion`: password reset, email verification, Firebase adapter, web routing/session/auth pages. 104 backend tests, 49 web tests. Browser walkthrough against the local backend: register → verify email → edit profile → logout → login → reset password → login with new password.
- **2026-09-23** — Phase 2 complete on branch `feat/phase2-providers` (stacked on the unmerged Phase 1 PR #1). 175 backend tests, 66 web tests. Browser check of discovery, detail and provider onboarding against the local backend.
- **2026-09-23** — Phase 2.5 complete on branch `feat/phase2-5-design-system` (from `main` after PR #2). 175 backend tests, 79 web tests. Browser walkthrough in Arabic and English, desktop and mobile widths.
- **2026-09-24** — Phase 3 complete on branch `feat/phase3-medical-jobs` (from `main` after PR #3): billing/entitlements foundation, audit log, contact-leak moderation, medical jobs and talent marketplace (backend + web + admin console). 268 backend tests, 129 web tests. Browser walkthrough in Arabic (desktop + 375px) and English: employer onboarding → verification → plan request → admin activation → job review → seeker résumé → apply (contact blocked, then accepted) → messaging → shortlist → interview → talent search → invitation. Walkthrough data deleted afterwards.
- **2026-09-24** — PR #4 Codex review: five commercial-correctness findings fixed on `feat/phase3-medical-jobs` (attempt-based usage references, employer row lock for concurrent limits, restore re-check, featured expiry rule, subscription expiry during renewal) plus two analogous rules (invitations, seats/plan requests). 284 backend tests, 129 web tests.
- **2026-09-24** — PR #4 Codex review round two (9 findings): contact-leak coverage for `detailed_specialty` and language names, bounded newest-first message threads, `jobs.application_review` gate on every employer applicant route, talent quota only after valid filters, same-row language/level filter, admin controls for suspended subscriptions, real pagination for my-applications and the employer workspace with a server-side active-job count. 309 backend tests, 137 web tests.
- **2026-09-24** — PR #4 Codex review round three (6 findings): withdrawal-reason leak check, invitation expiry before duplicate checks, safe reactivation next to a pending request (superseding policy), row locks on application/interview/invitation/subscription transitions, on-demand job picker pages, paginated seeker invitations. 327 backend tests, 142 web tests.
- **2026-09-24** — PR #4 Codex review round four (5 findings): identity fields locked after verification, withdrawal during INTERVIEW with interview cancellation, interview answers refused on closed applications, job row locks across the lifecycle, invitation guidance only after acceptance. 346 backend tests, 150 web tests.
- **2026-09-24** — PR #4 Codex review round five (3 findings): locked job edits, identity edits serialised with verification decisions, entitlement gates on saved-candidate/detail/invitation reads. 360 backend tests, 150 web tests.
- **2026-09-24** — PR #4 Codex review round six (4 findings): suspended employers blocked from applicant workflows, applications/invitations validated under employer+job locks with no charge on rejection, submit re-checks recruiting under the lock, public employer jobs paginated. 373 backend tests, 155 web tests.
- **2026-09-24** — PR #4 Codex review round seven (3 findings): locked subscription expiry, expired invitations never auto-accepted on apply, canonical talent-search billing signature. 383 backend tests, 155 web tests.
- **2026-09-24** — PR #4 Codex review round eight (3 findings): agency hiring-field invariant on resulting state, invitation acceptance validated under locks, candidate saves gated on recruiting status. 401 backend tests, 155 web tests.
- **2026-09-24** — PR #4 Codex review round nine (4 findings): pending subscriptions exposed separately from the effective one in the billing summary (entitlements untouched), retained agency hiring fields cleared explicitly by the job editor, facility `provider_profile` selectable during employer onboarding from the caller's own profile, and write-named throttle scopes (`jobs_create`, `talent_invite`) applied to POST only. 415 backend tests, 169 web tests; OpenAPI regenerated for `BillingSummary.pending_subscription`; no migration.
- **2026-09-25** — PR #4 Codex review round ten (3 findings): restore shares the full submission eligibility gate, administrators see the agency flag and the linked facility before verifying, job rows show the real submission time. 425 backend tests, 177 web tests; OpenAPI regenerated for the admin provider reference.
- **2026-09-25** — PR #4 Codex review round eleven (4 findings): approval shares the submit/restore publication gate, deadline rule on publication, authoritative active-slot capacity independent of read endpoints, eager-loaded nested employer relations on job cards. 444 backend tests, 177 web tests.
- **2026-09-25** — PR #4 Codex review round twelve (4 findings): activation refuses retired plans, decision notes bounded to the history column, messages serialised with application closure, public detail follows the search deadline rule. 460 backend tests, 177 web tests; OpenAPI regenerated for the 500-character decision note.
- **2026-09-25** — PR #4 Codex review round thirteen (4 findings): Django admin cannot drive the subscription lifecycle, saved candidates honour the shared visibility rule and are paginated, talent detail exposes the own saved-record id with an unsave action (plus a design-system fix so void actions report success). 471 backend tests, 181 web tests; OpenAPI regenerated.
